<div class="unassigned">
  {{#each todo in unassigned}}
    <div class="cards">
      <span class="todo_project">{{todo.project}}</span>
      <button {{action "assign" todo}} class="assign_btn"></button>
    </div>
  {{/each}}
</div>

<div class="assigned">
  {{#each todo in assigned}}
    <div class="cards">
      <span class="todo_project">{{todo.project}}</span>
      <span class="todo_status">{{todo.status}}</span>
    </div>
  {{/each}}
</div>