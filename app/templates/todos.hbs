<div class="unassigned">
  {{#each todo in unassigned}}
    <div class="cards">
      <span class="todo_project">{{todo.project}}</span>
    </div>
  {{/each}}
</div>

<div class="assigned">
  {{#each todo in assigned}}
    <div class="cards">
      <span class="todo_project">{{todo.project}}</span>
      <span class="todo_status">{{todo.status}}x</span>
    </div>
  {{/each}}
</div>